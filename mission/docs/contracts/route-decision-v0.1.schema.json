{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ao.mission.route-decision.v0.1",
  "type": "object",
  "required": [
    "schema",
    "mission_id",
    "route",
    "reason",
    "safe_to_execute"
  ],
  "properties": {
    "schema": { "type": "string", "const": "ao.mission.route-decision.v0.1" },
    "mission_id": { "type": "string" },
    "route": { "type": "string" },
    "reason": { "type": "string" },
    "safe_to_request": { "type": "boolean" },
    "safe_to_execute": { "type": "boolean" },
    "safe_to_promote": { "type": "boolean" },
    "exact_next_action": { "type": "string" }
  }
}
